# frozen_string_literal: true

class AddSubscriptableTo<%= table_name.camelize %> < ActiveRecord::Migration[<%= rails_version %>]
  def change
    add_column :<%= table_name %>, :stripe_customer_id, :string
    add_index :<%= table_name %>, :stripe_customer_id, unique: true
  end
end
