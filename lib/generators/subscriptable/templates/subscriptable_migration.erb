# frozen_string_literal: true

class Create<%= table_name.camelize %> < ActiveRecord::Migration[<%= rails_version %>]
  def change
    create_table :<%= table_name %> do |t|
      t.string :name, :string, null: false, index: true
      t.string :stripe_customer_id, index: true, null: false, unique: true

      t.timestamps
    end
  end
end
