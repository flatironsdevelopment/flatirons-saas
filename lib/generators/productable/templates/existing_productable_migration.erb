# frozen_string_literal: true

class AddProductableTo<%= table_name.camelize %> < ActiveRecord::Migration[<%= rails_version %>]
  def up
    add_column :<%= table_name %>, :name, :string, null: false
    add_index :<%= table_name %>, :name

    add_column :<%= table_name %>, :stripe_product_id, :string, null: false
    add_index :<%= table_name %>, :stripe_product_id, unique: true
  end

  def down
    raise ActiveRecord::IrreversibleMigration
  end
end
